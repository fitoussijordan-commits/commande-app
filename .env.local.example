# Optionnel — restreint le proxy Odoo à cette seule URL (protection SSRF renforcée).
# Si non défini, le proxy bloque quand même les IPs privées/locales par défaut,
# mais fixer ODOO_URL est recommandé en production.
ODOO_URL=https://monentreprise.odoo.com
